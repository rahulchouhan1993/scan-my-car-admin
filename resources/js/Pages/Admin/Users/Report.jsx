import DefaultLayout from '../../../layout/DefaultLayout'
import { route } from 'ziggy-js'

const Report = (props) => {
  return (
    <></>
  )
}

Report.layout = (page) => <DefaultLayout>{page}</DefaultLayout>
export default Report
